sudo: required

services:
  - docker

before_install:
  - docker build -t ggextra-image .

script:
  - docker run ggextra-image /bin/bash -c 'cd .. && R CMD build ggExtra && R CMD check $(ls | grep "tar\\.gz") --as-cran --no-manual'
  